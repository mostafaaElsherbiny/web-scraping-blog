{% extends 'base.html.twig' %}
 
{% block title %}Edit Blog{% endblock %}
 
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Blog</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_blog_index') }}"> 
                    View All Blog
                </a>
            </div>
            <div class="card-body">
                {% include 'blog/_form.html.twig' with {'button_label': 'Update Blog', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}
